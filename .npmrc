# Garante que devDependencies sejam instaladas mesmo com NODE_ENV=production
# (a Hostinger instala em produção, o que faria o npm pular devDeps e quebrar
# o build com "vite: command not found"). As ferramentas de build já estão em
# dependencies; isto é reforço para qualquer ferramenta dev necessária no build.
include=dev
